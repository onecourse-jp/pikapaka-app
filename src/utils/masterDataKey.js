// import { useSelector } from 'react-redux';

// export const getDataMasterDataByKey = (attributeKey, keyId) => {
//   const { mapData } = global.store.getState().masterDataReducer;

//   return mapData?.[attributeKey]?.[keyId];
// };
