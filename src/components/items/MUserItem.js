import * as React from "react";
import {useState} from "react";
import {TouchableOpacity, StyleSheet, View, Platform, Dimensions, ImageBackground} from "react-native";

let {width, height} = Dimensions.get("window");
width = width > 600 ? 600 : width;
export function MUserItem(props) {
  return <></>;
}
